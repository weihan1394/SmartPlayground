using brew install swig and re-running pip install pyscard did the job.

1. Install swig
https://www.dev2qa.com/how-to-install-swig-on-macos-linux-and-windows/

2. Install pyscard
https://pypi.org/project/pyscard/
pip install pyscard

3. Install sqlite3
https://www.tutorialspoint.com/sqlite/sqlite_installation.htm
# Make sure SmartPlaygroundDB.db is in the root folder

4. Install flask
- create controller folder first
https://dev.to/sahilrajput/install-flask-and-create-your-first-web-application-2dba
export FLASK_APP=SmartPlaygroundServer.py
python -m flask run

#examples
# print(rfid)
# # connect to database file
# dbConnect = sqlite3.connect("SmartPlaygroundDB.db")

# # assign row_factory to sqlite3.Row class
# dbConnect.row_factory = sqlite3.Row

# # create a cursor to work with db
# cursor = dbConnect.cursor()

# # execute basic command
# # cursor.execute("SELECT * FROM Game1Queue")
# cursor.execute('SELECT * from Game1Queue where inQueue= "1" AND rfidTag= ?', (rfid,))

# rows = cursor.fetchall()
# print(rows)
# numRows = len(rows)

# if (numRows >= 1):
#     # already in queue
#     return ("Already exist")
# else:
#     # not found
#     # check if the rfid tag is active
#     return ("true")